{
  "name": "votewise-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client-install": "cd client && npm install",
    "server-install": "cd server && npm install",
    "install-all": "npm run client-install && npm run server-install",
    "test": "npm run test --prefix client && npm run test --prefix server",
    "test:client": "npm run test --prefix client",
    "test:server": "npm run test --prefix server",
    "dev-client": "cd client && npm run dev",
    "dev-server": "cd server && npm run dev",
    "dev": "npm run install-all && echo '--- STARTING CLIENT AND SERVER ---' && start npm run dev-client && npm run dev-server",
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "start": "cd server && npm start"
  }
}
